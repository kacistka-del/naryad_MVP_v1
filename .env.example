# Адрес API. В dev можно оставить /api — Vite проксирует его на localhost:8080.
# В проде за nginx тоже достаточно /api.
VITE_API_BASE_URL=/api

# Куда Vite проксирует /api в режиме разработки
VITE_DEV_API_TARGET=http://localhost:8080
