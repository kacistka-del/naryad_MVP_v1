/**
 * Устанавливает собственный клиент данных в globalThis.__B44_DB__.
 *
 * Страницы и модули берут db именно из этого глобального значения,
 * поэтому модуль должен импортироваться самым первым в main.jsx.
 */
import { db } from '@/lib/self-hosted-db';

globalThis.__B44_DB__ = db;

export { db };
